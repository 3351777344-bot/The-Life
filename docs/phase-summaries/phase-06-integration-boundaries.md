# 阶段六：联调边界与可维护性

阶段时间：2026-05-18
阶段状态：已完成
对应分支：`Tilams`

## 阶段目标

进一步把前端从“本地可运行 demo”整理成“适合多人协作和后续真实联调”的结构，重点统一服务层边界、返回结构和模式配置。

## 对应 round

1. `2026-05-18-round-18`
2. `2026-05-18-round-19`

## 对应提交

1. `d3112ef` `feat: formalize integration boundaries and service contracts`
2. `3b7362d` `feat: strengthen service contracts and handoff docs`

## 本阶段完成内容

### 1. 服务模式配置独立化

新增：

1. `serviceConfig.js`

作用：

1. 统一描述当前服务运行模式
2. 为后续接入正式后端或切换到别的推演服务预留入口

### 2. 服务契约常量独立化

新增：

1. `serviceContracts.js`

集中管理：

1. 服务来源
2. 错误码
3. 元信息键名

### 3. 门面层元信息增强

`lifeJourneyFacade.js` 中的关键请求现在不只返回 `ok / data / error / meta`，还进一步统一了：

1. `mode`
2. `modeLabel`
3. `source`
4. `count`
5. `extractionMode`

### 4. 联调说明文档落地

新增：

1. `docs/INTEGRATION_HANDOFF.md`

用于帮助项目组成员快速理解：

1. 当前服务分层
2. 页面与能力映射
3. 推荐联调顺序
4. 统一结果结构

## 阶段成果

这一阶段结束后，前端在协作和联调层面更成熟了：

1. 运行模式更明确
2. 服务契约更清晰
3. 错误码和元信息更统一
4. 新成员接手时更容易上手

## 阶段遗留问题

1. 后续如果接正式后端，还需要继续扩展模式和 facade 分发逻辑
2. 部分字段契约仍可以再细化到更严格的接口级说明

## 下一阶段建议

后续最适合进入：

1. 最终收尾与验收
2. 针对当前初版做最后一轮流程和边界清点
3. 补一份当前版本完成度状态
