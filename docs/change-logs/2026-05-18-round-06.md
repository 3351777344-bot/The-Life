# 2026-05-18 Round 06

## 本轮目标

按照固定计划推进性能优化，先从最低风险、最直接的按页面异步加载入手，减少主包集中度。

## 修改文件

1. `src/App.vue`

## 改动摘要

### 1. 核心视图改为异步加载

把以下视图从同步静态导入改为 `defineAsyncComponent` 按需加载：

1. `GenesisView`
2. `DestinyView`
3. `DivergenceView`
4. `ComparisonView`
5. `ReflectionView`
6. `MentorshipView`
7. `ConclusionView`

### 2. 保持主流程行为不变

这轮没有改动页面业务逻辑，只调整了主入口如何装载这些视图，目标是先优化包结构，不干扰当前主流程闭环。

## 对产品行为的影响

1. 页面功能行为应保持一致。
2. 首次进入不同视图时会变成按需装载。
3. 为后续继续拆包和优化 chunk 结构打基础。

## 验证结果

本轮修改后需要重新执行构建验证，并观察打包产物变化。

## 后续建议

下一轮优先继续：

1. 检查拆包后的产物结构。
2. 视情况补充 loading 占位。
3. 继续评估 `three` 相关资源是否还能延迟加载。
